-- 0005_agent_events.down.sql — 回滚 agent 执行事件流
--
-- 注意：回滚会丢弃全部 agent 执行日志。这些是诊断信息而非业务数据 —— 任务
-- 状态、验证结果、PR 链接都在别的表里，不受影响 —— 所以丢了不影响系统运转，
-- 只是失去"当时 agent 到底干了什么"的追溯能力。
--
-- 回滚后详情页的执行日志面板会拿到 404，界面上表现为该面板不再出现。

DROP TABLE IF EXISTS agent_events;
