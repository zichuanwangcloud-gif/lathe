# 提交前检查清单

- [ ] 新增/改动的每个分支都有对应用例覆盖（包括错误路径，不只是happy path）。
- [ ] 测试名字能让失败信息本身讲清楚是哪个场景挂了。
- [ ] 没有测试之间共享可变全局状态导致的执行顺序依赖。
- [ ] `go test ./... -race` 在本地跑过一遍，没有新增的 data race。
- [ ] 需要外部依赖（DB/网络）的测试有清晰的 skip 提示，而不是直接超时或 panic。
